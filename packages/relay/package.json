{
	"name": "@activitypub-relay/relay",
	"version": "0.0.1",
	"type": "module",
	"main": "src/server.ts",
	"scripts": {
		"dev": "wrangler dev src/server.ts -e development",
		"deploy": "wrangler types && wrangler deploy --minify src/server.ts",
		"cf-typegen": "wrangler types",
		"db:generate": "drizzle-kit generate",
		"db:migrate": "wrangler d1 migrations apply relay --local",
		"db:migrate:prod": "wrangler d1 migrations apply relay --remote"
	},
	"imports": {
		"@/*": "./src/*"
	},
	"dependencies": {
		"@activitypub-relay/contract": "workspace:*",
		"@orpc/openapi": "^1.9.3",
		"@orpc/server": "^1.9.3",
		"drizzle-orm": "^0.45.2",
		"hono": "^4.9.9",
		"zod": "managed"
	},
	"devDependencies": {
		"@types/node": "^22.10.1",
		"drizzle-kit": "^0.31.10",
		"typescript": "^5.9.3",
		"wrangler": "^4.42.0"
	}
}
